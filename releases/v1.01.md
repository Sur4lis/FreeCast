# FreeCast v1.01

自由组合两个直播间的画面和声音，支持小窗、手动时间对齐和本地 AI 人声过滤。

支持 B站、虎牙、斗鱼、抖音，以及部分 HLS / FLV 直播源。独立弹幕接收与发送目前仅支持 B站。

下载 **FreeCast_v1.01.zip**，解压后在 Chrome / Edge 的扩展管理页开启开发者模式，选择“加载已解压的扩展程序”，加载包含 manifest.json 的文件夹。

旧版用户覆盖原插件目录后刷新扩展即可。AI 去人声为实验性功能，需要硬件 WebGPU 支持。
